
<h1>Welcome to Wholistic Group's Blog</h1>

<h3><a href="<%=new_post_path%>">Make a new post</a></h3>

<h2>All posts:</h2>
<% @posts.each do |post| %>
  <p><%= post.title%></p>
     <td><%= link_to 'Show', post_path(post) %></td>
      <td><%= link_to 'Edit', edit_post_path(post) %></td>
       <td><%= link_to 'Destroy', post_path(post),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
<% end %>
